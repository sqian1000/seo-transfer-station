# SEO Autonomous Daily Plan — 2026-05-05

- **时间**：2026-05-05 09:50:00
- **主控**：main
- **项目**：`project/seo-transfer-station`
- **输入**：`project/seo-transfer-station/reports/2026-05-05-agent-research.md`、`shared-context/intel/20260505-ai-api-seo-serp.md`、`shared-context/PROJECT_STATUS.md`
- **模式**：2xP1
- **原则**：今日不做首页/架构大改；基于 research 已形成最低可实现规格，因此可直接注册 dev03 小步 MDX 实现；qa04 等待 DEV final 后再注册，避免依赖未就绪导致假超时。

## 1. 今日选择

### P1-A：Claude Code settings/env 不生效 + `/model` 模型列表排查

- **页面归属**：`src/data/pages/claude-code-api-proxy.mdx`
- **目标关键词**：
  - Claude Code 中转站
  - Claude Code API Base URL
  - ANTHROPIC_BASE_URL 配置
  - ANTHROPIC_BASE_URL 不生效
  - Claude Code settings.json 优先级
  - Claude Code /model 看不到模型
- **用户意图**：用户已经知道要配置 Claude Code，但卡在“配了不生效、仍要求登录、仍走官方、模型列表不对”。
- **推荐内容结构**：
  1. 新增或强化 H2：`Claude Code 配了 ANTHROPIC_BASE_URL 但不生效怎么办？`
  2. 增加配置优先级表：Managed / CLI args / Local / Project / User；并说明 `.claude/settings.local.json`、`.claude/settings.json`、`~/.claude/settings.json` 的区别。
  3. 增加认证变量排查表：`ANTHROPIC_AUTH_TOKEN`、`ANTHROPIC_API_KEY`、`ANTHROPIC_BASE_URL`、shell profile、IDE 环境继承。
  4. 增加模型选择排查：`/model`、启动参数、`ANTHROPIC_MODEL`、settings `model`、provider 模型 allowlist。
  5. 增加最小验证步骤：查看当前 shell env、重启终端/IDE、用最小 prompt 验证、遇到 `model not found` 内链到排错页。
- **合规边界**：不要承诺“绕过封锁/绝不封号/100% 稳定”；不要写真实中转域名，继续使用 `YOUR_API_DOMAIN`。

### P1-B：Codex config.toml provider/base_url/wire_api 决策树 + OpenAI-compatible `/v1` 总表

- **页面归属**：`src/data/pages/codex-api-config.mdx`、`src/data/pages/openai-compatible-api.mdx`
- **目标关键词**：
  - Codex API 配置
  - Codex 第三方 API
  - Codex base_url
  - Codex CLI 中转站
  - ~/.codex/config.toml
  - OpenAI Codex custom base_url
  - OpenAI compatible base_url 要不要带 /v1
- **用户意图**：用户需要判断 Codex 到底该用 `openai_base_url`，还是新建 `[model_providers.<id>]`，以及 `wire_api` 与 `/v1` 怎么选。
- **推荐内容结构**：
  1. `codex-api-config.mdx` 新增字段决策树：
     - 只改内置 OpenAI provider 到代理：用 `openai_base_url`；
     - 新增第三方 provider：用 `[model_providers.<id>]`、`base_url`、`env_key`；
     - 要切换 provider：设置 `model_provider` 或使用 `--provider`；
     - Responses / Chat Completions 兼容问题：检查 `wire_api`。
  2. `codex-api-config.mdx` 增加最小 TOML 示例与冲突排查：用户级 `~/.codex/config.toml`、项目级 `.codex/config.toml`、trusted project、profile 覆盖。
  3. `openai-compatible-api.mdx` 新增 `/v1` 客户端差异表：Claude Code、Codex、Cline、Roo Code、Cursor、OpenAI SDK、LiteLLM、LM Studio。
  4. 增加内链：Claude Code 页、Codex 页、Cline/Roo 页、Cursor 页、401/model-not-found/429 排错页。
- **合规边界**：`/v1` 不给一刀切答案；按 provider 文档和客户端实际字段说明。

## 2. 今日注册策略

- **注册 dev03**：可执行，因为本计划已经给出文件、章节、关键词、验收口径与禁止事项。
- **不注册 pm01/content**：2026-05-01 至 2026-05-04 多次 timeout/ambiguous_success；今日 research 已由 main 和临时 Agent 补齐，继续等待会拖慢迭代。
- **不注册 arch02**：仅 MDX 内容增强，不涉及 Astro 组件、schema、路由或 sitemap 架构变化；若 DEV 发现长表移动端不可读，再新建 arch02 任务。
- **不注册 qa04**：依赖 dev03 final；DEV 未完成前无可验收产物。

## 3. dev03 任务验收口径

- 必须只修改 `project/seo-transfer-station/src/data/pages/*.mdx` 中相关页面；不改代码组件，除非发现移动端表格无法承载且先在 final 中说明。
- 必须保留 `YOUR_API_DOMAIN` 占位，不写真实线路域名。
- 必须包含真实字段名、路径、配置片段、验证步骤、常见错误。
- 必须更新或保留 title/description 与内链，不制造 noindex。
- 完成后运行：
  - `pnpm build`
  - 检查 sitemap 包含相关页面；
  - 检查首页与目标页不含 `noindex`；
  - grep 禁用绝对承诺词：`100%稳定|绝不封号|永久免费|官方唯一|保证可用`。
- final 需写入 `shared-context/agent-runs/dev03-20260505.jsonl`，并通过 guardrail update 为 completed。

## 4. 今日不做

- 不做中转站榜单/排行页。
- 不写真实价格、真实线路域名、稳定率、官方背书。
- 不提前注册 qa04。
- 不改 `/go/` noindex 策略。

## 5. Blocker

none。当前可进入 dev03 小步实现；QA 需等待 DEV final 后再注册。
