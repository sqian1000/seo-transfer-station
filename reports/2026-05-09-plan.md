# SEO Autonomous Daily Planning — 2026-05-09

- **时间**：2026-05-09 09:50:58
- **主控**：main
- **项目**：`project/seo-transfer-station`
- **输入**：`project/seo-transfer-station/reports/2026-05-09-agent-research.md`、`shared-context/intel/20260509-ai-api-seo-serp.md`、`shared-context/PROJECT_STATUS.md`
- **计划源**：`shared-context/tasks/today.json`
- **原则**：只注册依赖已就绪的可执行任务；QA 等 DEV final 后再注册，避免假超时。

## 1. 今日研究结论摘取

今日 SERP / 竞品机会继续集中在“配置已填但不生效”的高意图排错：

1. **Cursor custom OpenAI URL**：HTTP/1.1、Responses API / Chat Completions payload 错配、Override OpenAI Base URL 影响所有模型、`/v1/models` model id、curl 能通但 Cursor 不通。
2. **Claude Code VS Code/Cursor 扩展**：仍要求登录、不读 `ANTHROPIC_BASE_URL`、`disableLoginPrompt`、Extension settings、Claude settings/env、IDE 进程继承、gateway `/v1/models`。
3. **Codex CLI/IDE**：`config.toml`、`auth.json`、env、profile、CLI 参数、IDE 扩展能力限制的覆盖顺序。
4. **Cline/Roo**：基础配置已稳定，今日不抢优先级；后续 P2 补多 provider/profile 管理。
5. **模型真实性 / Key Pool 风险**：继续走可验证自测，不写泛榜单和绝对承诺。

## 2. 今日选择

### 选择模式：1xP0

今日选择 **1 个 P0 SEO 迭代任务**：

- `cursor-api-config.mdx` 增强 **Cursor 原生 provider 失败分型**。

### 为什么不是 2xP1

- 今日 research 明确 Cursor 方向最强：HTTP/1.1、Responses API / Chat Completions 错配、Override 全局影响、`/v1/models` model id 都是高意图、近转化问题。
- `vscode-cursor-plugin-guide.mdx` 与 `codex-api-config.mdx` 都是 P1，但可以排在 Cursor P0 之后，避免今天同时改三篇导致 QA 负担扩大。
- `PROJECT_STATUS.md` 仍有 2026-05-07 独立 QA gap；今天应控制实现范围，保留 QA 链路容量。

## 3. 已注册 / 未注册任务

### 已注册：DEV P0

| ACK | 责任方 | 状态 | 任务 | 产物 |
|---|---|---|---|---|
| `ACK-20260509-SEO-DEV03-001` | dev03 | request | 实现 Cursor 原生 provider 失败分型 P0 | `src/data/pages/cursor-api-config.mdx`、`shared-context/agent-runs/dev03-20260509.jsonl` |

任务边界：

- 只改：`project/seo-transfer-station/src/data/pages/cursor-api-config.mdx`
- 必须新增或增强：
  - Cursor 原生 provider 失败分型 H2/H3；
  - 入口混淆：Cursor 原生 AI vs Claude Code / Codex / Cline / Roo；
  - HTTP Compatibility Mode / HTTP/1.1；
  - Responses API vs Chat Completions payload/response mismatch；
  - Override OpenAI Base URL 可能影响全部模型；
  - model id 以 `/v1/models` 返回为准；
  - 401/404/429/timeout 与既有排错页内链；
  - `curl 能通但 Cursor 不通` 的最小定位步骤。
- 不得写真实 API Key、真实私有线路、价格、稳定率、官方背书、绝对承诺。
- 示例继续使用 `YOUR_API_DOMAIN` / 占位 Key。
- 运行 `pnpm build`。
- final 写入 `shared-context/agent-runs/dev03-20260509.jsonl`，并更新 guardrail 为 completed。

### 暂不注册：QA

| 责任方 | 预期 ACK | 原因 |
|---|---|---|
| qa04 | `ACK-20260509-SEO-QA04-001` | 依赖 DEV final 与产物；当前提前 register 会制造假超时。DEV final 后再注册。 |

### 暂不注册：PM / ARCH / CONTENT

| 责任方 | 原因 |
|---|---|
| pm01 | 今日规划输入已足够，且只选 1xP0 单页增强，不需要单独 PM 任务。 |
| arch02 | 只改 MDX 内容，无路由/schema/组件变更，不需要架构任务。 |
| content | 今日 research 已给出标题、关键词、来源和内容边界；可由 DEV 按计划执行，后续 QA 检查事实与安全表述。 |

## 4. Carry-over / Gap

- 2026-05-07 DEV final 已存在并经 main 于 2026-05-08 复跑 build pass。
- 仍缺 `ACK-20260507-SEO-QA04-001` 独立 QA final；登记为 carry-over QA gap。
- 今日不把 2026-05-07 QA 与 2026-05-09 DEV 混成同一个任务；后续 close 阶段优先补 QA 或明确阻塞原因。

## 5. 验收标准

DEV final 后，QA gate 应检查：

1. `pnpm build` 通过；
2. `dist/cursor-api-config/index.html` 存在；
3. sitemap 包含目标页，目标页无 noindex；
4. title/description/canonical 未破坏；
5. 内链指向已有 slug；
6. 无真实 API Key / 私有线路泄露；
7. 无“100% 真模型 / 绝对稳定 / 官方背书”等绝对承诺；
8. Cursor / Claude Code / Codex / Cline / Roo 没有混写；
9. 表格与长字段移动端可读。

## 6. 今日下一步

1. main 已写入 `shared-context/tasks/today.json`。
2. main 注册 `ACK-20260509-SEO-DEV03-001` request。
3. 由 dev03 执行 P0 MDX 内容增强。
4. DEV final 后再注册 `ACK-20260509-SEO-QA04-001`。
