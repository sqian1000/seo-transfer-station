# SEO Daily Plan — 2026-05-06

- **时间**：2026-05-06 09:50:00
- **主控**：main
- **项目**：`project/seo-transfer-station`
- **任务源**：`shared-context/tasks/today.json`
- **研究输入**：`project/seo-transfer-station/reports/2026-05-06-agent-research.md`、`shared-context/intel/20260506-ai-api-seo-serp.md`
- **规划方式**：main 读取 research/intel，并创建临时 PM+CONTENT、ARCH+QA 只读席位复核；最终由 main 统一决策与 register。

## 1. 今日选择

- **选择模式**：2xP1
- **不选单个 P0 的原因**：当前 P0 基础页已覆盖；今日新增机会更适合用两个低风险 MDX 内容增强承接，一个抓高转化工程长尾，一个补信任资产。
- **不做“中转站榜单”原因**：红海、质量参差且容易触发不可验证承诺；本站继续走工程配置、排错、可验证信任路线。

## 2. P1-A：Cursor / Cline / Roo 连接测试矩阵

- **优先级**：P1
- **目标页面**：
  - `project/seo-transfer-station/src/data/pages/cursor-api-config.mdx`
  - `project/seo-transfer-station/src/data/pages/cline-roo-code-config.mdx`
- **目标关键词**：
  - Cursor API 中转站
  - Cursor custom OpenAI URL
  - Cursor 自定义 API 连接失败
  - Cursor Claude Code 扩展 中转站
  - Cline OpenAI Compatible
  - Roo Code OpenAI Compatible
  - Roo Code tool calling 失败
  - Cline Roo Cursor 配置区别
- **内容规格**：
  1. 在 `cursor-api-config.mdx` 增强“先判断当前报错来自哪里”的诊断树：Cursor 原生 AI、Claude Code 扩展、Codex 扩展、Cline/Roo 扩展。
  2. 在 `cursor-api-config.mdx` 增强连接失败排查：配置位置、字段名、Base URL 样例、模型名、最小验证动作、失败下一跳。
  3. 在 `cline-roo-code-config.mdx` 增强 OpenAI Compatible 连接测试矩阵：Cline 与 Roo Code 分开写，突出 Roo Code 对 OpenAI native tool calling 的要求。
  4. 内链到 `openai-compatible-api.mdx`、`claude-code-api-proxy.mdx`、`codex-api-config.mdx`、401/model-not-found/429 排错页。
- **质量边界**：不要把 Cursor 原生 AI、Claude Code 扩展、Codex 扩展混成同一套配置；不要承诺所有 OpenAI-compatible 模型都支持 tool calling。

## 3. P1-B：模型真实性与降级判断清单

- **优先级**：P1
- **目标页面**：
  - 优先增强 `project/seo-transfer-station/src/data/pages/api-key-safety-checklist.mdx`
  - 如 DEV 判断内容过长，可只做安全页增强，不新建独立页面；独立 slug 后置。
- **目标关键词**：
  - AI API 中转站 模型真假
  - AI API 中转站 降级 判断
  - AI API 中转站 Key Pool 风险
  - Claude Code 中转站 风险
  - AI API 中转站 日志 安全
  - API Key 最小权限 分组预算
- **内容规格**：
  1. 写成“开发者自测清单”，不点名攻击具体服务商。
  2. 覆盖模型名映射、长上下文截断、tool calling、JSON/structured output、streaming、429/余额/并发、日志留存、Key 分组预算、敏感代码上传边界。
  3. 给出最小验证动作与安全建议：分项目 Key、预算上限、可轮换、不要上传客户数据/密钥/私有仓库。
  4. 内链到 API Key 安全、模型/费用、429、model not found、OpenAI-compatible 总页。
- **质量边界**：不得写真实线路、真实价格、稳定率、官方背书；不得承诺“绝对真模型/绝不降级/永不封号/100% 稳定”。

## 4. 今日 register 策略

### 已 register

| ACK | 责任方 | 状态 | 任务 | 依赖 |
|---|---|---|---|---|
| ACK-20260506-SEO-DEV03-001 | dev03 | request / accepted | 实现今日 2xP1 MDX 内容增强 | 今日 plan 与 research/intel 均已 ready |

### 未提前 register

- **qa04**：依赖 dev03 final；未有实现产物前不注册，避免假超时。
- **arch02**：今日仅 MDX 内容增强，无 Astro 组件、路由、schema 或信息架构硬改；若 DEV 决定新建独立页面/slug，再新建 arch/QA 前置任务。
- **pm01 / content**：本轮已由临时 PM+CONTENT 席只读复核并回传建议；考虑到近期持久链路不稳定，main 已形成最低可实现规格，避免阻塞。

## 5. DEV 验收标准

DEV final 至少包含：

1. 只修改目标 MDX 页面；不改 Astro 组件、路由、配置和依赖。
2. 长表拆短表/诊断树，避免移动端横向爆表。
3. 保留 `YOUR_API_DOMAIN` 或既有占位策略；不得写真实 Key、真实线路、真实价格、稳定率、官方背书。
4. 每个客户端段落包含：配置位置、字段名、Base URL 示例、模型名示例、最小验证动作、失败下一跳。
5. 运行 `pnpm build` 并记录结果。
6. final 写入 `shared-context/agent-runs/dev03-20260506.jsonl`，并通过 guardrail 更新 ACK 到 completed。

## 6. QA 后续标准

DEV final 后再注册 qa04。QA gate：

- `pnpm build`
- 目标 dist 页面存在
- sitemap / robots / noindex
- title / description / canonical
- 内链检查
- FAQ/JSON-LD 检查（如新增）
- 绝对承诺词检查
- 真实 API 域名 / API Key 泄露检查
- 移动端表格可读性

## 7. Blocker

none。Planning 已完成，dev03 可执行任务已 register；qa04 等 DEV final 后再 register。
